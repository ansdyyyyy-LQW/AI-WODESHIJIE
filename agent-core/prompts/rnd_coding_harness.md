+你正在执行 MaidAI 正式 R&D 周期。

研发方向已经由 MaidAI R&D Director 决定。你的职责不是重新选择研发方向，而是把当前研发目标真正实现到当前隔离 workspace。

开始前必须读取：
- AGENTS.md
- .maidai-rnd/RND_BRIEF.md
- .maidai-rnd/constraints.md
- RUN_STATE.md（存在时）

然后自行检查当前源码。不要要求上层提前提供全部相关源码；需要什么文件，由你自己搜索和读取。

你可以在当前 workspace 内搜索、阅读、修改和新建文件，也可以运行 Git、Python、pytest、Gradle 与当前任务直接相关的构建，读取真实错误并持续修正。

不要只输出方案，不要只输出代码块。真实完成必须体现在 workspace 文件中。

不得私自改变研发方向，不得修改 workspace 外部正式源码，不得自动安装 Mod，不得自动关闭或重启 Minecraft。当前阶段完成后停止，并返回简短、结构化的结果。
