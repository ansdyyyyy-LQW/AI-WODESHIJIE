# Capability Policy

默认 `strict_survival=true`。

允许：真实观察、移动、挖掘、放置、使用物品/方块、合成、熔炼、容器、进食、战斗、撤退、通用区域挖掘/放置、记忆和安全 Skill DSL。

禁止：地下矿物透视、无视遮挡远距实体读取、远程开箱、远程破坏/放置、紧急传送、强制拾取、生成物品、任意 NBT、直接命令执行、在生产世界 `eval` 未审查代码。

R&D 可以在隔离 worktree 中运行编译和测试，但不能写正式 `mods`，只能输出 Handoff。
