研发完成：{{ completed }}
需要人工操作：{{ requires_user_action }}

需要安装 Mod 时：
1. 关闭 Minecraft。
2. 把明确列出的 JAR 放入该实例的 `mods` 文件夹。
3. 安装清单中的依赖。
4. 按报告说明替换旧版，不能同时保留重复版本。
5. 重新启动 Minecraft，并进入原存档。
6. 在 Maid AI Control 中标记该 Handoff 已处理。
